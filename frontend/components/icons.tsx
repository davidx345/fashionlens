import { LucideProps, Package2 } from "lucide-react"
import * as React from "react"

export const Icons = {
  logo: (props: LucideProps) => (
    <Package2 {...props} />
  ),
  // Add other icons here as needed
}
